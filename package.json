{
  "name": "my-project-prettier",
  "version": "1.0.0",
  "description": "Configuration for Prettier in my project.",
  "devDependencies": {
    "husky": "^8.0.3",
    "lint-staged": "^16.1.2",
    "prettier": "^3.6.2"
  },
  "scripts": {
    "format:dbt": "prettier --write './**/*.{yml,yaml}'",
    "format:check:dbt": "prettier --check './**/*.{yml,yaml}'",
    "prepare": "husky install"
  },
  "lint-staged": {
    "./**/*.{yml,yaml}": [
      "prettier --write"
    ]
  }
}